# Fallback chain of popularity sources, tried in order (see --sources).
# Deezer needs nothing below — any source whose credentials are blank here
# is skipped automatically, no need to comment it out.

# Free, instant, self-serve key — no app review: https://www.last.fm/api/account/create
# (LASTFM_APIKEY is also accepted as an alias. Last.fm's paired "secret" is
# for the write/session auth flow — not needed here, so there's no
# LASTFM_SECRET variable to set.)
LASTFM_API_KEY=

# Discogs: https://www.discogs.com/settings/developers (free, instant, no app review)
# Use EITHER a personal access token (DISCOGS_TOKEN, or just DISCOGS_KEY with
# DISCOGS_SECRET left blank) OR a Consumer Key + Secret pair (DISCOGS_KEY +
# DISCOGS_SECRET, issued if you registered an "Application" instead) — only
# one of the two is needed.
DISCOGS_TOKEN=
DISCOGS_KEY=
DISCOGS_SECRET=

# Only needed for --sources including "spotify". Client Credentials flow —
# no user login/redirect URI needed, just create a free app:
# https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# Used as the User-Agent/contact for MusicBrainz + Discogs API calls, per
# their usage policies. Just a text string, not an account — any email works:
# https://musicbrainz.org/doc/MusicBrainz_API/Rate_Limiting
MUSICBRAINZ_CONTACT_EMAIL=
